apply from: '../../mixed-library.gradle'

dependencies {
    supportApi "com.android.support:support-annotations:$SUPPORT_ARTIFACT_VERSION"
    androidxApi "androidx.annotation:annotation:$androidx_version"

    androidxApi "androidx.constraintlayout:constraintlayout:$constraint_layout_version"
    supportApi "com.android.support.constraint:constraint-layout:$constraint_layout_version"

    api project(':static:commons-base')
}
